version: "3.9"

services:

  backend:
    build:
      context: ./backend
    container_name: shopsphere-backend
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    container_name: shopsphere-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend